/// @description Insert description here
// You can write your code in this editor
if yi > y-distance yi --;

if frame < time frame++ else effect = true;

if effect == true
{
	#region
	if type == 0
	{
		play.ring+=10;	
	}
	
	#endregion
	
	instance_destroy(id);
}